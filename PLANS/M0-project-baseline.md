# M0：项目基线整理

## 目标

把开发仓库整理为可持续迭代的项目结构，并把架构文档、Skills、测试样例和长期规则放入仓库。

## 范围

- 创建 `docs/`、`PLANS/`、`.agents/skills/`、`tests/`、`sample-vault/`、`schemas/`、`scripts/`。
- 迁移两份架构文档。
- 保留 4 个中文化 Skill 初稿。
- 创建 `AGENTS.md`、`README.md`、`DEVLOG.md`。

## 不做

- 不开发插件。
- 不接入真实 Vault。
- 不修改用户旧笔记。

## 验收

- 仓库目录清晰。
- `AGENTS.md` 约束明确。
- Skills 和测试样例在新仓库中可见。
- Git 仓库已初始化。
