# 任务日期范围验收

- 点击右上角“新建任务”，确认开始日期和结束日期均为可弹出日历的日期选择器。
- 选择相同开始/结束日期创建单日任务，确认任务 Markdown 中 `date` 与 `end_date` 相同。
- 选择跨日范围创建任务，确认 `date` 为开始日期、`end_date` 为结束日期。
- 尝试选择早于开始日期的结束日期，确认界面限制选择或保存时给出错误提示。
- 右键已有任务进入编辑，确认两个日期均能回显并可修改。
- 打开一个没有 `end_date` 的旧任务，确认仍能正常显示和编辑，结束日期默认回显为开始日期。
